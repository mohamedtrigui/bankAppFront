import { PipeTypeOpPipe } from './pipe-type-op.pipe';

describe('PipeTypeOpPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTypeOpPipe();
    expect(pipe).toBeTruthy();
  });
});

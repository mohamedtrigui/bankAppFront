import { PipeNumToArrayPipe } from './pipe-num-to-array.pipe';

describe('PipeNumToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeNumToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
